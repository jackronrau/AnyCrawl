

const estimatePromptToken = (model: string, text: string | string[]) => { }

const estimatePromptTokenCost = (model: string, inputTokens: number) => { }

export {
    estimatePromptToken,
    estimatePromptTokenCost
}